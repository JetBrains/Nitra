using Nitra.DependentProperties;

using Nemerle.Collections;
using Nemerle.Text;
using Nemerle.Utility;

using System;
using System.Collections.Generic;
using System.Console;
using System.Linq;

using Test;

module Program
{
  Main() : void
  {
    def root = Test.Run(D1(D2(D3()), 42));
    WriteLine("root.Attr0=" + root.Attr0);
    WriteLine("root.Attr1=" + root.Attr1);
    WriteLine();
    def _root = Test.Run(Kaboom3(Kaboom2(Kaboom1()), Kaboom2(Kaboom1()), Kaboom2(Kaboom1()), Kaboom2(Kaboom1())));
    _ = ReadKey();
  }
}
