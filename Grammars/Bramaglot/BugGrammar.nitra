syntax module BugGrammar
{
  using Whitespaces;
  using CStyleComments;

  extend token IgnoreToken
  {
    | [ExplicitSpaces] SingleLineComment;
    | [ExplicitSpaces] MultiLineComment;
  }

  [StartRule]
  syntax E = "E" "{" "}" !Any;
}