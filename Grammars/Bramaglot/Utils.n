using Nitra;
using System;

namespace Bramaglot
{
  public class Utils
  {
    public Test() : void
    {
      def text = "";
      def parseResult = Gherkin.Feature.Parse(SourceSnapshot(text));

      if (parseResult.IsSuccess)
        Console.WriteLine(GherkinParseTree.Feature.CreateParseTree(parseResult).Value());
      else
        foreach (error in parseResult.GetErrors())
        {
          def (line, col) = error.Location.StartLineColumn;
          Console.WriteLine($<#($line, $col): $(error.Message)#>);
        }
    }
  }
}
