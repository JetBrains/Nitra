using Nemerle;
using Nemerle.Imperative;
using Nemerle.Collections;
using Nemerle.Text;
using Nemerle.Utility;

using System;
using SCG = System.Collections.Generic;
using System.Linq;
using N2.Internal;

namespace N2.Runtime
{
  public abstract class WalkerHost
  {
    public abstract Type : WalkerTypeDescriptor { get; }

    private _astWalkerDescriptors : Hashtable[GrammarDescriptor, GrammarWalkerDescriptor] = Hashtable();

    public virtual FindGrammarWalkerDescriptor(grammar : GrammarDescriptor) : GrammarWalkerDescriptor
    {
      ignore(grammar);
      throw NotImplementedException();
    }
  }
}
