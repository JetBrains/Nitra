using Nemerle;
using Nemerle.Collections;
using Nemerle.Text;
using Nemerle.Utility;

using System;
using System.Collections.Generic;
using System.Linq;

namespace N2.Runtime
{
  public interface IComilerMessags
  {
    Warning      (loc : Location, msg : string, num : int = -1) : void;
    Hint         (loc : Location, msg : string, num : int = -1) : void;
    Error        (loc : Location, msg : string, num : int = -1) : void;
    FatalError[T](loc : Location, msg : string, num : int = -1) : void;
    Warning      (loc : Located,  msg : string, num : int = -1) : void;
    Hint         (loc : Located,  msg : string, num : int = -1) : void;
    Error        (loc : Located,  msg : string, num : int = -1) : void;
    FatalError[T](loc : Located,  msg : string, num : int = -1) : void;
    RootWarning  (loc : Location, msg : string, num : int = -1) : IRootComilerMessags;
    RootHint     (loc : Location, msg : string, num : int = -1) : IRootComilerMessags;
    RootError    (loc : Location, msg : string, num : int = -1) : IRootComilerMessags;
    RootWarning  (loc : Located,  msg : string, num : int = -1) : IRootComilerMessags;
    RootHint     (loc : Located,  msg : string, num : int = -1) : IRootComilerMessags;
    RootError    (loc : Located,  msg : string, num : int = -1) : IRootComilerMessags;
  }
}
