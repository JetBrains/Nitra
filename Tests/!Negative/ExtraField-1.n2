syntax module M1
{
  using Whitespaces;

  syntax X = "x"
  {
    private Field1() : int; //E:A private extra field must have a body

    Field2() : int; //E:An abstract extra field must be defined in an extention point rule only
  }
  
  syntax Y
  {
    private Field1() : int; //E:A private extra field can be declared only in simple rules and extention rules
    
    | Z = "z"
      {
        Field2() : int = 0; //E:A public extra field must be defined in an extention point rule or a simple rule only
      }
  }
}
